#include "main.h"
/**
 * handle_octal - print octal numbers and return the print count
 * @str: the string formatter.
 * @value: The value to be printed.
 * Return: the number of characters printed except NULL
 */
int handle_octal(const char *str, char *value)
{
	printf("\n handle_octal : String is : %s and value is :%s\n", str, value);
	return (0);
}
