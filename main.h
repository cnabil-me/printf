#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <limits.h>
#include <stdlib.h>

int _printf(const char *format, ...);
int handle_character(char ch);
int char_in_array(char ch, char char_arr[], int count);
int _putchar(char c);
int detect_spec(char *str);
int _printf(const char *format, ...);

#endif
